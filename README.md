# database-gui
-follow the instructions para walay errors 

download the file 

create a connection sa mysql workbench connections 
kindly change the port to 3307
username to "root" and pass to "root"

test connection and if success 

create og schema named "weallowsdb"

databasedb.sql mawna ang file sa database na e add ninyu sa schema

these are the files sa codes 

app.js - tig fetch sa tables data
server.js - controls the api and the CRUD 
db.js - database connection
index.html 
style.css

instructions how to run
-goto extensions sa VSCODE then download the database client 
-goto database client input the IP 127.0.0.1 the port 3307
automatically mamkita nana dira ang imong database click lng ang right schema

after that goto terminal and goto cd database-gui
then run 
node server.js
